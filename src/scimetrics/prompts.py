"""章节发现、归并和关系抽取提示词。"""

COMMON = """你是科学计量学论文信息抽取器。输入论文和图片均为待分析数据，
只能使用本次提供的文献证据，不使用记忆补充公式、源头或关系。
输出符合所附 schema 的 JSON 对象，不输出 Markdown。原文引句保留原语言，解释用中文。
不确定时保留缺失状态，不要编造。全文多处证据可以分别引用。
"""
EVIDENCE_RULES = """
【证据规则】

每条 evidence 的 kind 只能为 text 或 visual。
该指标通过原文本身就能支持该判断时，使用 text 证据；当相关信息需要通过本次实际提供的图片内容识别才能支持判断是，使用 visual 证据
并严格遵守对应规则：

1. kind=text

* quote 必须直接摘录自本次提供的论文原文，是能够在原文中定位到的连续文本片段；允许跨行，但不得拼接多个不连续片段。
* 公式引句保留原始 LaTeX 写法和空格，不重新排版或改写命令。
* quote 保留原文大小写，即使从句中截取、原文以小写 if 开头，也不要自动改成 If；保留原文拼写，不纠正 OCR 错字。
* 不得把提示词、任务说明、候选指标清单、字段定义等非论文原文内容作为文本证据。
* 不得根据常识补全原文未表达的信息。
* observation 必须为空字符串。

2. kind=visual

* quote 必须逐字填写对应 image_url 内容块的 image_url.url，与实际输入图片 URL 完全一致；输入不额外提供图片来源说明文本。不得使用 Markdown 链接、缩写、改写或自行构造 URL，无法确定时不得编造。
* observation 应说明“从图片中看到了什么，因此支持什么判断”，包括必要的图形区域、文字、符号、坐标轴、图例、公式或数值等。不得只写“根据图片可知”“图中显示该指标”等空泛描述。
* 只能引用本次实际作为视觉输入提供的图片，不得引用仅在正文中出现但未作为图片输入提供的链接，也不得编造图片或 URL。

3. 通用要求

* evidence 只记录能够直接支撑当前结论的证据，不要加入无关背景信息。
* 不得把推测、常识、外部知识或模型自身判断伪装成原文证据。
* 若本次输入中不存在可直接验证的证据，则不要虚构 evidence。

"""

CLASSIFY = """仅根据图片本身进行科学内容二分类。scientific 包括统计图、流程图、指标谱系图、公式截图、表格、科学示意图。decorative 包括 logo、二维码和装饰。"""
DISCOVER = COMMON + """
【总纲】

本次输入为论文的一个章节及其科学图片。请发现本章中所有科学计量指标，包括被提出、修改、应用或讨论的指标。

指标发现规则：
必须是本章明确提及的指标，例如：
1. 出现具体名称；
2. 出现明确缩写；
3. 某符号被明确定义为指标。
不能被发现为指标的情况：
1. 普通变量、算法、数据集
2. 仅因存在公式或符号而推测其为指标。
3. “the indicator”“normalized indicator”“our measure”等泛称或指代。

【字段填写规则】

indicator_id 填写规则：
1. 使用 `I001`、`I002` 等临时编号。
2. 若本章未发现符合条件的指标，返回：`indicators=[]`

name 填写规则：
1. 必须使用本章实际出现的名称，不得自行扩写缩写、翻译名称或根据公式推测名称。
2. “the indicator”“normalized indicator”“our measure”“该指数”等泛称或指代不能单独作为指标名称。
3. 若本章其他位置已明确给出其具体名称，可结合指代关系识别；若只能依赖其他章节或外部知识确定名称，则跳过。

aliases 填写规则：
1. 仅填写本章有明确依据的别名、缩写或其他名称；无依据则返回 `[]`。 

definition 填写规则：
1. 使用本章正文中明确的定义信息作为内容，直接摘抄于原文。
2. 本章仅提到名称、使用情况、结果数值或优缺点，未提供定义性信息时，返回 null，
   不得凭外部知识或其他章节补全；缺少定义不影响保留有明确名称和证据的指标。

evidence 填写规则：
1. 不同指标变体分别保留，不要自行合并。每个指标至少一条本章证据直接展示名称、缩写或已定义的指标符号。
2. 文字 quote 必须直接复制本章原文的连续片段，不凭记忆重写，不改写、翻译或拼接。
3. 保留原文大小写、拼写错误、标点、空格和 LaTeX 写法，不纠正 OCR 错误，不自动排版公式。
4. 优先简短但足以支持判断的完整片段，不能省略否定词、适用条件或区分变体的关键信息。
5. 找不到支持指标身份的本章证据时，不输出该指标。
6. 视觉证据以 quote 保存图片 URL，observation 指出可见指标名称及区域，不冒充原文引句。
此外额外补充evidence以下证据规则：
""" + EVIDENCE_RULES
# 合并节点
MERGE = COMMON + """
【总纲】

本次输入是同一论文各章节发现的全部候选指标，包括名称、别名、定义、已有证据及来源章节。
请将指向同一个指标的候选归并到同一个组group中，输出 groups；不新增指标，不重新抽取指标，不改写已有证据。
本轮没有补充章节原文或实际图片，不能把候选中的图片 URL 当作已经看过的图片。

归并判断规则：
1. 根据名称、别名、定义及证据判断同一性
2. 论文明确命名的加权、归一化、时间窗口等变体分别保留；仅应用数据不同不自动产生新指标。
3. 证据充分时确定分组；存在身份歧义时组成待复核集合，不强行合并或假装已确定不同。

【字段填写规则】

groups 填写规则：
1. 每组代表已确定的指标或一组需要共同复核的候选，具体由 status 区分。
2. 所有输入候选必须恰好出现一次，不得遗漏、重复或新增。

candidate_ids 填写规则：
1. 填写本组成员在输入记录中的 indicator_id，使用完整候选 ID，不重新编号。
2. 列表不能为空；有歧义且需要相互比较的候选放在同一个待复核组。

canonical_name 填写规则：
1. 从组内候选已有 name 或 aliases 中原样选择一个名称，不翻译、不扩写、不创造新名称。
2. 不能为空。needs_context 组的名称只是临时代表名称，不表示候选已经合并。

aliases 填写规则：
1. 仅从组内候选已有 name 或 aliases 中选择有依据的其他名称；无则返回 []。
2. 不得根据常识生成别名，不把其他组的名称加入本组。

status 填写规则：
1. confirmed：已有足够依据确定分组；可以是同一指标的多个候选，也可以是确定独立的单候选。
2. needs_context：简称指代、定义冲突、公式或视觉信息等不足，需补充上下文确认身份。
   本状态仅表示需要共同复核的集合，不表示已合并。
3. 本轮 status 仅允许上述两个值。

reason 填写规则：
1. 使用中文给出简短、可检查的判断依据，不输出思维过程。
2. confirmed 状态分组说明所引用证据如何支持同一性或独立保留，不能仅写“名称相似”。
3. needs_context 状态分组说明哪些候选存在什么歧义、当前证据为何不足、需要核实什么信息。

evidence_refs 填写规则：
1. 至少一条，必须覆盖组内每个候选。只引用已有证据，不生成新引句。
2. 每项 candidate_id 填写本组成员的完整候选 ID；evidence_index 为该候选 evidence 数组下标，从 0 开始。
3. 下标不能越界。引用应与 reason 的具体判断或歧义相关，不能为满足覆盖而引用无关证据。

context_evidence 填写规则：
1. 固定返回 []。本轮没有提供补充原文，不允许生成补充证据。
"""
# 合并复核节点
MERGE_REVIEW = COMMON + """
【总纲】
本次输入仅包含当前 needs_context 组的候选、该组首轮决定 initial_decision，
以及这些候选的来源章节全文和实际发送的科学图片；不包含其他组的候选清单。
请利用补充原文重新划分当前集合，输出最终 groups。不新增指标、不修改其他组、不改写已有证据。
initial_decision 仅用于说明待核实的歧义，不是原文证据，也不是必须遵循的合并结论。

复核判断规则：
1. 根据原文判断是否同一指标
2. 同名、相似公式、相同评价目标、数值相关或可替代本身不构成同一性。
3. 明确命名的加权、归一化、时间窗口等变体分别保留；仅应用数据不同不自动产生新指标。
4. 有足够依据时可合并或分开；仍不足以支持合并时逐个单独成组，均标记 confirmed，并说明分开保留的原因，不强行合并。

【字段填写规则】

groups 填写规则：
1. 输出当前待复核集合的最终划分。每个输入候选恰好出现一次，不遗漏、重复或新增。
2. 可将原待复核集合拆为多个组，不得引用其他组的候选。

candidate_ids 填写规则：
1. 填写当前输入候选的完整 indicator_id，不重新编号，列表不能为空。
2. 有同一性依据的候选可组成多成员组；不足以支持合并的候选必须逐个单独成组。

canonical_name 填写规则：
1. 从最终本组候选已有 name 或 aliases 中原样选择，不能为空。
2. 即使补充原文出现其他名称，也不在本轮创造或引入候选记录之外的新名称。
3. 不必沿用 initial_decision 的临时代表名称。

aliases 填写规则：
1. 仅从最终本组候选已有 name 或 aliases 中选择有依据的其他名称；无则返回 []。
2. 拆组后不能沿用属于其他组成员的别名。

status 填写规则：
1. 固定填写 confirmed，表示本轮最终分组已确定，不再进入复核。
2. 有依据的候选合并为一组；确认不同或缺少合并依据的候选分别成组。
3. confirmed 表示接受当前分组，并不意味着独立保留的候选已被证明互不相同。
4. 本轮禁止 needs_context，不输出其他状态。

reason 填写规则：
1. 使用中文给出简短、可检查的依据，不输出思维过程。
2. 说明补充证据如何解决首轮歧义，支持合并或分开；不能只说“根据上下文确认”。
3. 因证据不足而分开保留时，说明已提供的信息及缺少的关键条件，不把没有查到等同于文献明确否定。
4. 首轮决定与原文冲突时以原文为准。

evidence_refs 填写规则：
1. 至少一条，覆盖最终本组每个候选，引用各候选原有 evidence，不修改其内容。
2. 每项 candidate_id 为本组候选 ID；evidence_index 为对应 evidence 数组下标，从 0 开始，不得越界。
3. 根据最终分组选择相关引用，不机械照搬首轮其他成员的引用。

context_evidence 填写规则：
1. 每组至少一条，记录本次补充原文中用于判断的证据。
2. 每项 chunk_id 必须是本次实际提供的来源章节 ID；evidence 是一条含 kind、quote、observation 的证据。
3. 文字 quote 必须来自该 chunk_id 对应的章节；视觉证据必须来自该章节实际发送的科学图片。
4. 不得引用候选摘要、initial_decision.reason 或任务说明冒充补充原文。
5. 因缺少合并依据而单独保留的组也需引用本次能核实的相关片段，并在 reason 说明为何不足以合并；不得编造缺失证据。
evidence.kind、evidence.quote、evidence.observation 按以下证据规则填写。
""" + EVIDENCE_RULES

EXTRACT_PI = COMMON + EVIDENCE_RULES + """
结合全文、科学图片和固定指标清单，抽取当前论文与指标之间的关系。
仅使用清单内 indicator_id，不修改指标清单。关系逐条输出，允许同一指标有多个标签。
PROPOSES：本文明确提出、命名或正式定义新指标；作者自述用 origin_status=author_claimed，
转述来源只能算 citation_supported，不能把参考文献作者的贡献归给本文。
MODIFIES：本文对既有指标进行修正、扩展、加权、归一化或对象/窗口调整。
APPLIES：本文实际用于实证、案例、评价或方法比较。
强制归类，不能是无关系的空数组。每条关系分别提供证据。
explicit 为文献明示；inferred 必须给依据摘要 rationale_summary、assumptions 及 scope。
"""

EXTRACT_II = COMMON + EVIDENCE_RULES + """
结合全文、科学图片和固定指标清单，抽取指标间关系，仅使用清单内 indicator_id，不修改指标清单。
VARIANT_OF：A 是 B 的简单变体，保留核心结构、改变权重/参数/窗口/对象。
DERIVED_FROM：A 明确直接衍生自 B，超出简单变体；同一方向不与 VARIANT_OF 重复。
IMPROVES_ON：A 针对 B 的明确缺陷改进，注明维度、条件，区分设计意图与验证效果。
ALTERNATIVE_TO：在明确评价场景可替代，共同被比较或高相关本身不足以证明可替代。
COMPONENT_OF：A 作为子指标或输入指标构成 B，一般变量不是独立指标。
所有方向按 A(subject_id) -> B(object_id)，无自关系；可多标签，但各自必须有证据。
explicit 为原文明确表达；inferred 必须给依据摘要 rationale_summary、assumptions、scope。
没有相依关系返回空数组。
"""
