# 项目约定

- 用户要求不新增测试文件或测试代码。使用语法、导入、配置及必要的实际输入检查验证修改。
- 新增或修改 `src/scimetrics/prompts.py` 中的任务提示词时，采用“总纲＋字段填写规则”的结构，参照 DISCOVER 和 EVIDENCE_RULES。
- 总纲先说明任务、实际输入范围、输出目标及整体判断规则；随后按实际 schema 逐字段说明来源、填写方式、允许值、空值和禁止事项。嵌套字段也要说明。
- 仅需要输出原文/视觉证据的提示词追加 EVIDENCE_RULES；引用已有证据下标的字段单独说明引用规则。
- 不通过继承另一阶段的完整提示词再追加相反规则。首轮与复核分别描述输入和状态约束，保持与 schema、校验代码一致。
